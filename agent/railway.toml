[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["python"]
pythonVersion = "3.11"

[deploy]
startCommand = "uvicorn src.agent:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
