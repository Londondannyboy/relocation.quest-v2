# =============================================================================
# VERCEL ENVIRONMENT VARIABLES
# Copy this to .env.local for local development
# Set these in Vercel Dashboard > Settings > Environment Variables for deployment
# =============================================================================

# Database (Neon)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Hume EVI (Voice)
HUME_API_KEY=your-hume-api-key
HUME_SECRET_KEY=your-hume-secret-key
NEXT_PUBLIC_HUME_CONFIG_ID=your-hume-config-id

# Neon Auth (if using)
NEON_AUTH_BASE_URL=https://your-project.neonauth.region.aws.neon.tech/database/auth

# Zep (Memory)
ZEP_API_KEY=your-zep-api-key
ZEP_ACCOUNT_ID=your-zep-account-id
ZEP_GRAPH=relocation-quest

# Agent URL (Railway backend)
AGENT_URL=https://your-railway-app.up.railway.app/agui
NEXT_PUBLIC_AGENT_URL=https://your-railway-app.up.railway.app/agui
